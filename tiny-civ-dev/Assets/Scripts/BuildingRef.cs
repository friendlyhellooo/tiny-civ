using UnityEngine;

public class BuildingRef : MonoBehaviour
{
    // (!)  This is to hold a reference to the scriptable object so it knows
    //      what data to give to which building
    public BuildingData data;
}
